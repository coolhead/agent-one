services:
  api:
    build: ../apps/api
    env_file: ../.env
    ports: ["8000:8000"]
    volumes:
      - ../apps/api/.chroma:/app/.chroma
  ui:
    build:
      context: ../apps/ui
    ports: ["8501:5173"]
    environment:
      - VITE_API_URL=http://localhost:8000
